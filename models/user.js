class User {
    constructor(idNode,id, userName, password, score) {
            this.idNode = idNode
            this.id = id
            this.userName = userName
            this.password = password
            this.score = score
    }
}

module.exports = User;